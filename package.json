{
  "name": "codex-links",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler pages dev public",
    "deploy": "wrangler pages deploy public --project-name codex-links",
    "kv:seed": "node scripts/seed-demo.mjs",
    "cloud:check": "node scripts/check-cloud-setup.mjs",
    "cloud:install-secrets": "zsh scripts/install-pages-secrets.sh",
    "cloud:save-config": "node scripts/save-cloud-config.mjs",
    "cloud:sync-repos": "node scripts/sync-cloud-repos.mjs",
    "smoke:release": "node scripts/smoke-release.mjs",
    "rollback:prepare": "node scripts/revert-last-good.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.82.2"
  }
}
